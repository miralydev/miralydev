#    Ruziev Miraly
`Systems & C Programmer`

```c
#include <stdio.h>

int main(void)
{
    printf("Exploring the depths of the Linux Kernel...\n");
    return 0;
}
```

### Core Stack
* **Languages:** `Pure C` / `Asm` 
* **Toolchain:** `GCC` / `Clang` / `Make` / `Linkers`
* **Environment:** `Arch Linux` / `Vim` / `QEMU`
* **Communication:** `Mailing Lists (LKML)` / `Git send-email`
* **MORE**
---

### Kernel Contributions
* **Subsystems:** `drivers/staging/`
* **Patches:** 
  * [Active] `staging: rtl8723bs: fix msleep warning in core` -> [Lore Archive](https://kernel.org)

---

```text
[miraly@bluetooth ~]\$ shutdown -h now
```
